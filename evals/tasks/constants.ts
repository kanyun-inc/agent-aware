/**
 * 任务相关常量
 */

/**
 * 默认测试应用路径
 *
 * 这是一个包含基本 React 应用的测试夹具，用于评估 SDK 和 Server 的集成。
 * 如需重新生成，可以使用 `pnpm create vite` 创建新的 React 项目并集成 @reskill/agent-aware。
 */
export const DEFAULT_TEST_APP_PATH = 'examples/test-app-1738394400';
