name: Evals

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch: # 支持手动触发

jobs:
  eval:
    name: Run Evaluations
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Run Evaluations
        id: eval
        run: |
          pnpm eval --trials 5 --parallel 2>&1 | tee eval-output.txt
          exit_code=${PIPESTATUS[0]}
          echo "exit_code=$exit_code" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Generate Summary
        run: |
          # 读取最新的 Markdown 报告
          REPORT_FILE=$(ls -t evals/results/*.md 2>/dev/null | head -1)
          
          if [ -f "$REPORT_FILE" ]; then
            echo "## 📊 Agent-aware 评估报告" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            cat "$REPORT_FILE" | tail -n +2 >> $GITHUB_STEP_SUMMARY
          else
            echo "## ❌ 评估报告生成失败" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "请检查评估日志获取详细信息。" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            
            // 查找最新的 Markdown 报告
            const resultsDir = 'evals/results';
            let reportContent = '';
            
            try {
              const files = fs.readdirSync(resultsDir)
                .filter(f => f.endsWith('.md'))
                .sort()
                .reverse();
              
              if (files.length > 0) {
                const reportPath = path.join(resultsDir, files[0]);
                reportContent = fs.readFileSync(reportPath, 'utf8');
              }
            } catch (e) {
              console.log('No report file found:', e.message);
            }
            
            // 构建评论内容
            let body = '## 📊 Agent-aware 评估结果\n\n';
            
            if (reportContent) {
              // 移除标题（第一行）
              const lines = reportContent.split('\n');
              body += lines.slice(1).join('\n');
            } else {
              body += '⚠️ 评估报告生成失败，请检查 Actions 日志。\n';
            }
            
            body += '\n\n---\n';
            body += `*Generated by [Agent-aware Evals](${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID})*`;
            
            // 查找并更新或创建评论
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            
            const botComment = comments.find(c => 
              c.user.type === 'Bot' && 
              c.body.includes('Agent-aware 评估结果')
            );
            
            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body,
              });
            }

      - name: Upload Eval Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: eval-results
          path: |
            evals/results/*.json
            evals/results/*.md
            evals/results/transcripts/
          retention-days: 30

      - name: Check Eval Status
        if: steps.eval.outputs.exit_code != '0'
        run: |
          echo "::error::评估未全部通过，请检查报告详情"
          exit 1
