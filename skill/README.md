# Agent-aware Skill

监控用户在 Web 应用中的行为，自动发现并修复问题的 Cursor Agent Skill。

## 何时使用

**必须使用（自动启用）**：
1. ✅ 生成或修改 Web 应用代码后
2. ✅ 修改关键交互逻辑（表单、按钮、路由等）
3. ✅ 用户报告 UI 问题（点击无响应、报错等）

**建议使用**：
4. 💡 开发调试阶段优化用户体验
5. 💡 性能优化时分析交互热点

**适用场景**：React、Vue、Next.js、原生 HTML 等 Web 应用（开发环境）

## 功能

**基础功能**：
- ✅ 查询用户行为数据（点击、滚动、悬停、编辑）
- ✅ 识别挫折信号（愤怒点击、无效点击）
- ✅ 获取行为摘要和热点区域

**高级功能**：
- ✅ 代码生成后自动启动监控
- ✅ 检测运行时错误和用户挫折行为
- ✅ 主动分析并修复问题
- ✅ 轻量级文件监听，低开销
- ✅ 累积问题记录，延迟测试也能捕获

## 文件结构

```
skill/
├── SKILL.md                    # 完整使用指南（基础 + 高级）
├── README.md                   # 本文件
├── INSTALLATION.md             # 详细安装指南
└── scripts/
    └── monitor.sh              # 轻量级监听脚本
```

## 快速开始

### 1. 安装依赖包

```bash
# 安装 Server（全局，推荐）
npm install -g @reskill/agent-aware-server

# 在用户项目中安装 SDK（开发依赖）
npm install --save-dev @reskill/agent-aware
```

📖 **详细安装说明**：[INSTALLATION.md](./INSTALLATION.md)

### 2. 启动 Server

```bash
npx agent-aware-server start
```

### 3. Agent 使用此 skill

当 agent 生成或修改 Web 代码时，会自动：

- 清空旧数据
- 启动 2 分钟监控
- 发现问题立即修复
- 监控结束给出报告

3. **手动测试监听脚本**

```bash
bash skill/scripts/monitor.sh 60 3
# 监听 60 秒，每 3 秒检查一次
```

## 前置条件

使用此 skill 前，必须确保：

1. ✅ **Server 已安装**：全局或项目级别
2. ✅ **SDK 已在用户项目中安装**：作为开发依赖
3. ✅ **Server 正在运行**：`npx agent-aware-server start`

**检查命令**：
```bash
# 检查 Server
npm list -g @reskill/agent-aware-server

# 检查 SDK（在用户项目中）
npm list @reskill/agent-aware
```

📖 **详细说明**：[INSTALLATION.md](./INSTALLATION.md)

## 工作原理

### Server 端（主动检测）

```
收到行为/错误数据
    ↓
实时计算指标（frustrationScore, errorCount 等）
    ↓
超过阈值？
    ↓ YES
写入 .agent-aware/alert.json（严重问题）
写入 .agent-aware/issues.json（所有问题）
```

### Agent 端（轻量级监听）

```
启动监听脚本（每 5 秒检查文件）
    ↓
alert.json 存在？
    ↓ YES
读取问题详情
    ↓
查询 HTTP API 获取完整数据
    ↓
分析并修复
    ↓
删除 alert.json，重新监控
```

## 检测规则

| 条件 | 严重级别 | 行动 |
|------|---------|------|
| `errorCount >= 1` | critical | 立即修复 |
| `frustrationScore >= 70` | critical | 立即修复 |
| `50 <= frustrationScore < 70` | warning | 记录问题 |
| `rageClickCount >= 3` | warning | 记录问题 |
| `deadClickCount >= 2` | warning | 记录问题 |

## 示例输出

### 开始监控

```
✅ 代码已生成！我会监控 2 分钟，请测试页面。

🔍 监控中...
⏱️  已监控 30/120 秒...
```

### 发现问题

```
⚠️ 发现问题并已修复：

**问题**：Submit 按钮点击无响应（5 次愤怒点击）
**原因**：缺少 loading 状态
**修复**：已添加 disabled={isLoading} 和 spinner

请刷新页面继续测试。
```

### 监控完成

```
📊 监控报告

✅ 运行状况：良好（挫折指数 15/100）
💡 建议：表单可添加更明确的验证提示

需要我应用优化吗？
```

## 技术实现

### Server 端

- **IssueDetector 类**（`packages/server/src/detector/issueDetector.ts`）
  - 计算问题严重级别
  - 写入 alert.json 和 issues.json
  - 实现冷却期机制（避免告警风暴）

- **HTTP API 集成**（`packages/server/src/httpApi.ts`）
  - POST /behaviors 和 POST /errors 触发检测
  - GET /issues 查询累积问题
  - DELETE /issues 清空问题

### 测试覆盖

- ✅ 67 个测试全部通过
- ✅ 单元测试：IssueDetector 类
- ✅ 集成测试：HTTP API + IssueDetector
- ✅ TDD 开发流程

## 相关文档

- **Skill**：[SKILL.md](./SKILL.md)
- **Spec**：[SPEC-SRV-004: Issue Detector](../specs/server/004-issue-detector.md)

## License

MIT
